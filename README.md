# topic-classifier
A baseline topic classifier trained model based on DNNs

See the notebook.ipynb for example run on collab

To run:

`python run.py --config config/config.yaml`

where in config you can select train or eval
     
